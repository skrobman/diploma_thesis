name: SAST - Bandit

on:
  workflow_dispatch:
  push:
    branches:
      - master-branch
  pull_request:

permissions:
  contents: read

jobs:
  bandit:
    name: Scan Python code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install Bandit
        run: |
          python -m pip install --upgrade pip
          pip install bandit

      - name: Run Bandit
        run: |
          bandit -r . \
            -x ./.venv,./venv,./migrations,./tests
